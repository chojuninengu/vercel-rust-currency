{
  "name": "vercel-rust-currency",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "start": "vercel dev"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  }
} 